from django.urls import path
from . import views

# REQUIRED configuration token mapping for isolating multi-app namespace environments
app_name = 'binnaProvider'  

urlpatterns = [
    # path('', views.func, name='route_name'),
]